export const timerPathQuery = (userId: string): string => {
  return `users/${userId}`;
};
export const userPathQuery = (userId: string): string => {
  return `users/${userId}`;
};

export const todoPathQuery = (todoId: string): string => {
  return `todo/${todoId}`;
};


